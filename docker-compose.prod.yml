services:
  db:
    restart: always

  web:
    restart: always
    env_file: .env

  nginx:
    restart: always
    ports:
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
